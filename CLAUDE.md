# このリポジトリでのClaude運用ルール

## 議事録の維持（最優先ルール）

**会話の中で「重要そうなこと」が出てきたら、必ず `MEETING_NOTES.md` に追記してください。**

- 追記のタイミング: **決定・合意・方針変更・重要な文脈が明らかになった直後**（会話終わりまで待たない）
- 追記のフォーマット: 既存構成（トピック別 + セッション記録）を維持
  - 該当セクションが既にあればそこに追記
  - 新しいトピックなら新セクション追加
  - 最下部の「セッション記録」に日付付きで **その日の主な変更・議論** を1〜3行で残す
- `MEETING_NOTES.md` は gitignored（ローカル・非公開）なので、機微情報も書いてよい
- 更新後、ユーザーに **「議事録に追記しました」と明示的に報告** すること

## 追記すべき「重要そうなこと」の判断基準

以下は必ず追記:

- **NEXT EXPO の戦略意図・目標・制約**（例: 学校ルート回避、委託費獲得意図など）
- **技術判断とその理由**（採用したもの & 意図的に採用しなかったもの）
- **メディア・パートナー関連の情報**（誰と接触した、誰が取り上げてくれた、コラボ提案）
- **予算・費用・料金プラン**の話
- **未解決事項・保留・宿題**
- **仕様変更**（bot の挙動を変える判断）
- **重要な事実発見**（例: 「◯◯メディアが実は連載を持っている」等）

以下は追記不要:

- ルーチンなbug fix・typo修正
- 単発の質問回答（それ自体が方針や事実の変更を伴わない）
- ツール使用の詳細
- ユーザーとの雑談

## 関連ファイル

- `README.md` — 公開向けbot説明 & LINE友だち追加URL
- `MEDIA_STRATEGY.md` — メディアコラボ4フェーズプラン（**gitignored**）
- `MEETING_NOTES.md` — このプロジェクトの意思決定・戦略ログ（**gitignored**）
- `bot/main.py` — bot本体
- `.github/workflows/` — cron ワークフロー2種

## その他の運用ルール

- 機微情報を含むファイルは必ず `.gitignore` に追加する
- ローカル開発は Python 3.13（3.14 は ensurepip 壊れているため使わない）
- state.json 手動更新時のgit操作順: `git add` → `git commit` → `git pull --rebase` → `git push`
- push 前には他の workflow による state.json 更新が来ていないか `git pull --rebase` で確認
